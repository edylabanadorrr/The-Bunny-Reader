{
  "name": "the-bunny-reader",
  "version": "1.0.0",
  "scripts": {
    "build": "cp src/*.html src/*.css src/*.js dist/",
    "deploy": "git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "html-minifier": "^4.0.0"
  }
}
